<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.school.project">

    <!-- 在SDCard中创建与删除文件权限 -->
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS"
        tools:ignore="ProtectedPermissions" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".message.MessageDetailsActivity"></activity>
        <activity android:name=".offer.OfferListActivity" />
        <activity android:name=".position.PositionDetailsActivity" />
        <activity android:name=".position.PositionEditActivity" />
        <activity android:name=".position.CompanyDetailsActivity" />
        <activity android:name=".position.PositionListActivity" />
        <activity android:name=".resume.ResumeDetailsActivity" />
        <activity android:name=".resume.ResumeEditActivity" />
        <activity android:name=".resume.ResumeListActivity" />
        <activity
            android:name=".user.UserDetailsActivity"
            android:windowSoftInputMode="stateVisible" />
        <activity
            android:name=".HomeActivity"
            android:launchMode="singleTask" />
        <activity android:name=".user.RegisterActivity" />
        <activity android:name=".user.LoginActivity" />
        <activity android:name=".base.BaseActivity" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.Theme_Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>